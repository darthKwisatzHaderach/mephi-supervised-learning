Давайте разберём **пример с квадратичной зависимостью** — то есть когда истинная связь между признаком \(x\) и целевой переменной \(y\) описывается **квадратичной функцией**, например:

\[
y = a x^2 + b x + c
\]

Мы покажем, как обучить такую модель с помощью **градиентного спуска**, шаг за шагом, включая:
- выбор модели,
- определение параметров,
- функцию потерь,
- вычисление градиентов,
- обновление параметров.

---

## 🔹 Шаг 1. Исходные данные (пример из жизни)

Представим, что мы измеряем **высоту мяча**, брошенного вертикально вверх, в разные моменты времени:

| Время \(x\) (с) | Высота \(y\) (м) |
|------------------|------------------|
| 0.0              | 0.0              |
| 0.5              | 4.8              |
| 1.0              | 8.1              |
| 1.5              | 9.9              |
| 2.0              | 10.2             |
| 2.5              | 8.8              |
| 3.0              | 5.9              |

Физика говорит: высота зависит от времени по закону  
\[
h(t) = v_0 t - \frac{1}{2} g t^2,
\]  
то есть — **квадратичная функция**: \( y \approx \theta_2 x^2 + \theta_1 x + \theta_0 \).

Наша задача — **найти параметры** \( \theta_0, \theta_1, \theta_2 \), которые наилучшим образом описывают эти данные.

---

## 🔹 Шаг 2. Выбираем модель

Мы задаём **гипотезу** (параметрическую модель):

\[
h_\theta(x) = \theta_0 + \theta_1 x + \theta_2 x^2
\]

Это всё ещё **линейная модель по параметрам** (линейная комбинация параметров), хотя и **нелинейная по признаку** \(x\). Это важно: **градиентный спуск работает, потому что модель линейна относительно \(\theta\)**.

---

## 🔹 Шаг 3. Функция потерь

Как и раньше, используем **среднеквадратичную ошибку** (MSE):

\[
J(\theta_0, \theta_1, \theta_2) = \frac{1}{2m} \sum_{i=1}^{m} \big( h_\theta(x^{(i)}) - y^{(i)} \big)^2
\]

где \( m = 7 \) — количество измерений.

---

## 🔹 Шаг 4. Градиентный спуск: формулы обновления

Нужно вычислить частные производные \( \frac{\partial J}{\partial \theta_j} \) для \( j = 0,1,2 \).

Поскольку:

\[
h_\theta(x^{(i)}) = \theta_0 + \theta_1 x^{(i)} + \theta_2 (x^{(i)})^2
\]

то при дифференцировании по каждому параметру получаем:

### Производная по \( \theta_0 \):
\[
\frac{\partial J}{\partial \theta_0} = \frac{1}{m} \sum_{i=1}^m \big( h_\theta(x^{(i)}) - y^{(i)} \big) \cdot \underbrace{\frac{\partial h_\theta}{\partial \theta_0}}_{=1}
= \frac{1}{m} \sum_{i=1}^m (h_\theta(x^{(i)}) - y^{(i)})
\]

### Производная по \( \theta_1 \):
\[
\frac{\partial J}{\partial \theta_1} = \frac{1}{m} \sum_{i=1}^m \big( h_\theta(x^{(i)}) - y^{(i)} \big) \cdot \underbrace{\frac{\partial h_\theta}{\partial \theta_1}}_{=x^{(i)}}
= \frac{1}{m} \sum_{i=1}^m (h_\theta(x^{(i)}) - y^{(i)}) \cdot x^{(i)}
\]

### Производная по \( \theta_2 \):
\[
\frac{\partial J}{\partial \theta_2} = \frac{1}{m} \sum_{i=1}^m \big( h_\theta(x^{(i)}) - y^{(i)} \big) \cdot \underbrace{\frac{\partial h_\theta}{\partial \theta_2}}_{=(x^{(i)})^2}
= \frac{1}{m} \sum_{i=1}^m (h_\theta(x^{(i)}) - y^{(i)}) \cdot (x^{(i)})^2
\]

> ✅ **Каждый параметр умножается на ту часть, с которой он входит в модель**.

---

## 🔹 Шаг 5. Правила обновления

Пусть скорость обучения \( \eta = 0.01 \) (маленькая, потому что \( x^2 \) может быть большим).

Тогда на каждой итерации:

\[
\begin{aligned}
\theta_0 &:= \theta_0 - \eta \cdot \frac{1}{m} \sum (h - y) \\
\theta_1 &:= \theta_1 - \eta \cdot \frac{1}{m} \sum (h - y) \cdot x \\
\theta_2 &:= \theta_2 - \eta \cdot \frac{1}{m} \sum (h - y) \cdot x^2
\end{aligned}
\]

---

## 🔹 Шаг 6. Пример вычисления (первая итерация)

**Инициализация:**  
\( \theta_0 = 0,\ \theta_1 = 0,\ \theta_2 = 0 \) → модель предсказывает 0 везде.

**Ошибки:**  
\( h - y = -y \), то есть:  
\[
[-0.0,\ -4.8,\ -8.1,\ -9.9,\ -10.2,\ -8.8,\ -5.9]
\]

**Градиенты:**

- \( \frac{\partial J}{\partial \theta_0} = \frac{1}{7}(-0 -4.8 -8.1 -9.9 -10.2 -8.8 -5.9) \approx \frac{-47.7}{7} \approx -6.814 \)
- \( \frac{\partial J}{\partial \theta_1} = \frac{1}{7}( -4.8 \cdot 0.5 -8.1 \cdot 1.0 -9.9 \cdot 1.5 -10.2 \cdot 2.0 -8.8 \cdot 2.5 -5.9 \cdot 3.0 ) \approx \frac{-72.35}{7} \approx -10.336 \)
- \( \frac{\partial J}{\partial \theta_2} = \frac{1}{7}( -4.8 \cdot 0.25 -8.1 \cdot 1.0 -9.9 \cdot 2.25 -10.2 \cdot 4.0 -8.8 \cdot 6.25 -5.9 \cdot 9.0 ) \approx \frac{-158.85}{7} \approx -22.693 \)

**Обновление (η = 0.01):**

\[
\begin{aligned}
\theta_0 &= 0 - 0.01 \cdot (-6.814) = 0.068 \\
\theta_1 &= 0 - 0.01 \cdot (-10.336) = 0.103 \\
\theta_2 &= 0 - 0.01 \cdot (-22.693) = 0.227
\end{aligned}
\]

Новая модель:  
\[
h(x) = 0.068 + 0.103 x + 0.227 x^2
\]

Уже не ноль! С каждой итерацией она будет всё лучше описывать параболу.

---

## 🔹 Шаг 7. Что получится в итоге?

После нескольких сотен итераций модель сойдётся к чему-то вроде:

\[
h(x) \approx -4.9 x^2 + 10.1 x + 0.0
\]

— что соответствует физической модели \( h(t) = v_0 t - \frac{1}{2} g t^2 \) с \( g \approx 9.8\ \text{м/с}^2 \), \( v_0 \approx 10.1\ \text{м/с} \).

---

## 🔹 Важный вывод

Хотя зависимость **нелинейна по \(x\)**, модель всё ещё **линейна по параметрам** (\(\theta_0, \theta_1, \theta_2\)) — поэтому **градиентный спуск работает точно так же**, просто добавляется ещё один параметр и соответствующий градиент.

Это называется **полиномиальной регрессией** — мощный способ моделировать нелинейные зависимости, **не выходя за рамки линейных методов**.

---

## 💡 Как обобщить?

Если вы хотите смоделировать любую зависимость вида:

\[
y = \theta_0 + \theta_1 \phi_1(x) + \theta_2 \phi_2(x) + \dots + \theta_n \phi_n(x)
\]

— где \(\phi_j(x)\) — любые функции от \(x\) (например, \(x^2\), \(\sin x\), \(\log x\)),  
то градиент по \(\theta_j\) будет:

\[
\frac{\partial J}{\partial \theta_j} = \frac{1}{m} \sum_{i=1}^m (h_\theta(x^{(i)}) - y^{(i)}) \cdot \phi_j(x^{(i)})
\]

То есть **каждый параметр "чувствует" свою функцию признака**.

### 🔍 Точная формулировка:

- **Градиентный спуск — это общий метод численной оптимизации.**  
  Он может применяться к **любой** дифференцируемой функции — линейной или нелинейной.

- **Линейность по параметрам** (например, \( h_\theta(x) = \theta_0 + \theta_1 x + \theta_2 x^2 \)) — это **частный, удобный случай**, потому что:
  - Функция потерь \( J(\theta) \) становится **выпуклой**,
  - Градиент имеет **простую аналитическую форму**,
  - **Гарантирована сходимость** к глобальному минимуму (при правильном learning rate).

- **Но если модель нелинейна по параметрам** (например, \( h_\theta(x) = \theta_0 \cdot e^{\theta_1 x} \) или нейросеть), то:
  - Градиентный спуск **всё равно применим**,
  - Однако:
    - \( J(\theta) \) **может быть невыпуклой**,
    - Возникают **локальные минимумы, седловые точки**,
    - Нет гарантии сходимости к **глобальному** минимуму,
    - Может потребоваться **автоматическое дифференцирование** (autograd), так как аналитический градиент сложен.

---

### 🧠 Почему в учебных материалах (в том числе в вашем уроке) делают акцент на линейных по параметрам моделях?

Потому что:
1. Это **простейший рабочий пример**, на котором можно:
   - Вывести градиент **вручную**,
   - Понять **механику обновления параметров**,
   - Визуализировать **поверхность ошибки**,
   - Убедиться, что метод действительно **работает надёжно**.
2. Это основа для всего остального: полиномиальная регрессия, линейные модели с признаками-преобразованиями и т.д.
3. Даже в нейросетях **каждый слой линейный по своим параметрам** (а нелинейность вносится активациями между слоями).

---

### ✅ Итог: уточнённое понимание

| Утверждение | Верно? | Пояснение |
|------------|--------|----------|
| Градиентный спуск требует, чтобы модель была линейной по параметрам | ❌ **Нет** | Он работает и с нелинейными по параметрам моделями |
| Градиентный спуск **гарантированно находит глобальный минимум** только если модель линейна по параметрам (и используется MSE) | ✅ **Да** | Потому что тогда функция потерь выпуклая |
| В учебных примерах используют линейные по параметрам модели, чтобы упростить понимание | ✅ **Да** | Это дидактический выбор, а не техническое ограничение |

---

### 💡 Аналогия

Представьте, что градиентный спуск — это **метод спуска с горы в тумане**, используя только **чувствительность подошв** (градиент).

- Если гора — **гладкий купол** (выпуклая функция, как в линейной регрессии), вы **гарантированно дойдёте до самой низкой точки**.
- Если гора — **Альпы** (невыпуклая функция, как в нейросети), вы можете **застрять в долине**, которая не самая низкая, но «локально хорошая».

Но **метод спуска в обоих случаях один и тот же** — просто **гарантии разные**.
