Давайте **с нуля, по порядку**, шаг за шагом выведем всё, что нужно для понимания **линейной регрессии и градиентного спуска** — от модели до обновления параметров.

---

## 📌 Шаг 1. Что мы хотим сделать?

У нас есть **данные**:  
- \( x^{(i)} \) — признак (например, рост в метрах),  
- \( y^{(i)} \) — целевая переменная (например, вес в килограммах).

Мы хотим **научиться предсказывать** \( y \) по \( x \).

---

## 📌 Шаг 2. Выбираем модель

Предположим, что зависимость **линейная**. Тогда наша модель — это **линейная функция**:

\[
h_\theta(x) = \theta_0 + \theta_1 x
\]

Здесь:  
- \( h_\theta(x) \) — **предсказание** модели для входа \( x \),  
- \( \theta_0 \) — смещение (bias, свободный член),  
- \( \theta_1 \) — вес (наклон, насколько сильно \( y \) меняется при изменении \( x \)).

> 🔹 Мы не знаем \( \theta_0 \) и \( \theta_1 \). Наша задача — **найти такие \( \theta_0, \theta_1 \), чтобы \( h_\theta(x^{(i)}) \) было как можно ближе к \( y^{(i)} \)**.

---

## 📌 Шаг 3. Как измерить «близость»? → Вводим функцию потерь

Разница между предсказанием и истиной:  
\[
\text{ошибка}_i = h_\theta(x^{(i)}) - y^{(i)}
\]

Но:
- Ошибки могут быть положительными и отрицательными → компенсируют друг друга.
- Нужна **всегда положительная** мера.

👉 Поэтому **возводим в квадрат** и **усредняем** по всем примерам:

\[
J(\theta_0, \theta_1) = \frac{1}{2m} \sum_{i=1}^{m} \big( h_\theta(x^{(i)}) - y^{(i)} \big)^2
\]

Разберём:
- \( m \) — число примеров (объектов в выборке),
- \( \frac{1}{m} \) — **усреднение** (чтобы ошибка не зависела от размера данных),
- \( \frac{1}{2} \) — **технический приём**: при дифференцировании квадрата появится множитель 2, который сократится. Это упрощает формулу градиента.

> Цель: **минимизировать** \( J(\theta_0, \theta_1) \).

---

## 📌 Шаг 4. Как минимизировать \( J \)? → Градиентный спуск

Функция \( J(\theta_0, \theta_1) \) — это **поверхность** в 3D (по осям \( \theta_0, \theta_1, J \)).  
Мы хотим спуститься в **самую низкую точку** (минимум).

**Идея градиентного спуска**:  
> Двигаться в сторону, противоположную **градиенту** — вектору частных производных.

### Обновление параметров:

\[
\theta_j := \theta_j - \eta \cdot \frac{\partial J}{\partial \theta_j}, \quad j = 0, 1
\]

Где:
- \( \eta \) — **скорость обучения** (learning rate),
- \( \frac{\partial J}{\partial \theta_j} \) — насколько быстро растёт ошибка, если чуть изменить \( \theta_j \).

---

## 📌 Шаг 5. Находим частные производные

Напомним:  
\[
h_\theta(x^{(i)}) = \theta_0 + \theta_1 x^{(i)}
\]

Подставим в \( J \):

\[
J = \frac{1}{2m} \sum_{i=1}^m \big( \theta_0 + \theta_1 x^{(i)} - y^{(i)} \big)^2
\]

### 🔹 Производная по \( \theta_0 \)

Применим **цепное правило**:

\[
\frac{\partial J}{\partial \theta_0} = \frac{1}{2m} \sum_{i=1}^m 2 \big( h_\theta(x^{(i)}) - y^{(i)} \big) \cdot \frac{\partial}{\partial \theta_0} h_\theta(x^{(i)})
\]

Но:
\[
\frac{\partial}{\partial \theta_0} h_\theta(x^{(i)}) = \frac{\partial}{\partial \theta_0} (\theta_0 + \theta_1 x^{(i)}) = 1
\]

Поэтому:
\[
\frac{\partial J}{\partial \theta_0} = \frac{1}{m} \sum_{i=1}^m \big( h_\theta(x^{(i)}) - y^{(i)} \big)
\]

✅ **Нет \( x^{(i)} \)** — потому что \( \theta_0 \) не умножается на \( x \).

---

### 🔹 Производная по \( \theta_1 \)

Аналогично:

\[
\frac{\partial J}{\partial \theta_1} = \frac{1}{2m} \sum_{i=1}^m 2 \big( h_\theta(x^{(i)}) - y^{(i)} \big) \cdot \frac{\partial}{\partial \theta_1} h_\theta(x^{(i)})
\]

Теперь:
\[
\frac{\partial}{\partial \theta_1} h_\theta(x^{(i)}) = \frac{\partial}{\partial \theta_1} (\theta_0 + \theta_1 x^{(i)}) = x^{(i)}
\]

Поэтому:
\[
\frac{\partial J}{\partial \theta_1} = \frac{1}{m} \sum_{i=1}^m \big( h_\theta(x^{(i)}) - y^{(i)} \big) \cdot x^{(i)}
\]

✅ **Появилось \( x^{(i)} \)** — потому что \( \theta_1 \) умножается на \( x^{(i)} \), и производная «выносит» этот множитель.

---

## 📌 Шаг 6. Итоговые формулы обновления

Подставляем градиенты в правило обновления:

\[
\theta_0 := \theta_0 - \eta \cdot \frac{1}{m} \sum_{i=1}^m \big( h_\theta(x^{(i)}) - y^{(i)} \big)
\]

\[
\theta_1 := \theta_1 - \eta \cdot \frac{1}{m} \sum_{i=1}^m \big( h_\theta(x^{(i)}) - y^{(i)} \big) \cdot x^{(i)}
\]

> Эти формулы применяются **одновременно** на каждой итерации (важно: обновлять оба параметра **по старым значениям** — не по частично обновлённым).

---

## 📌 Шаг 7. Почему это работает?

- Если предсказание **меньше** истинного (\( h < y \)), то ошибка отрицательна → мы **увеличиваем** \( \theta_0 \) и \( \theta_1 \) (чтобы поднять предсказание).
- Если предсказание **больше** (\( h > y \)), ошибка положительна → мы **уменьшаем** параметры.
- Умножение на \( x^{(i)} \) в обновлении \( \theta_1 \) делает так, что **примеры с большим \( x \)** сильнее влияют на изменение наклона — что логично: именно там \( \theta_1 \) проявляется сильнее.

---

## ✅ Кратко: цепочка рассуждений

1. **Модель**: \( h_\theta(x) = \theta_0 + \theta_1 x \)  
2. **Цель**: сделать \( h_\theta(x^{(i)}) \approx y^{(i)} \)  
3. **Мера ошибки**: \( J = \frac{1}{2m} \sum (h - y)^2 \)  
4. **Минимизация**: двигаемся против градиента \( \nabla J = \left( \frac{\partial J}{\partial \theta_0}, \frac{\partial J}{\partial \theta_1} \right) \)  
5. **Производные**: получаются из правила дифференцирования сложной функции + того, как \( \theta_j \) входит в модель  
6. **Обновление**: корректируем параметры шаг за шагом, пока ошибка не станет минимальной.
